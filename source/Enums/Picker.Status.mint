/* An enum to represent the state of a picker. */
type Ui.Picker.Status {
  /* The picker is focused. */
  Focused

  /* The picker is focused and the dropdown is open. */
  Open

  /* The picker is idle (not focused, not open). */
  Idle
}
