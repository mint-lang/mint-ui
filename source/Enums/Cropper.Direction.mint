/* An enum which describes the dragging direction. */
enum Ui.Cropper.Direction {
  /* Dragging the right or bottom side. */
  Backward

  /* Dragging the left or top side. */
  Forward

  /* Moving the whole area. */
  Move
}
